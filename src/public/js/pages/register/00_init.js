const form_register = document.getElementById('form_register');
const button_register = document.getElementById('button_register');
const notification_error = document.getElementById('notification_error');
const box_success = document.getElementById('box_success');
const input_password = document.getElementById('input_password');
const password_checklist = document.getElementById('password_checklist');

fePasswordChecklistBind(input_password, password_checklist);
